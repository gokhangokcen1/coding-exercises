#include <stdio.h>
#include <stdlib.h>
#include <math.h>

//exp(x) : e üzeri
//exp2(x) : 2 üzeri
//sqrt(x) : karekök
//pow(x,y) : x uzerý y 
//log(x) : e tabanýnda log 
//cbrt(x) : küpkök - ondalýklý sayý döner
//(double) ceil(x) : yukarý yuvarla (float)
//(double) floor(x) : aþaðý yuvarla (float)
//(double) fabs(x) : ondalýklý sayýnýn mutlak deðeri
//(int) abs(x) : int mutlak deðer 
//(double) fdim(x,y) : sayýlarýn farký pozitifse göster, negatifse 0
//(double) fma(x,y,z) : (x+y)*z 
//(double) fmax(x,y) : büyük olan
//(double) fmin(x,y) : küçük olan 
//(double) fmod(x,y) : x%y
//(double) hypot(x,y) : hipotenüsü verir
//isgreater isgreaterequal



int main(){

	//isfinite : sonsuzsa 0, degilse 1 doner isinf
//	double x = 19, y = 0.12, z = 7.14/0.00;
//	
//	printf("%.2f degeri sonsuz bir deger%s\n",x,isfinite(x) ? " degildir":"dir");
//	printf("%.2f degeri sonsuz bir deger%s\n",y,isfinite(y) ? " degildir":"dir");
//	printf("%.2f degeri sonsuz bir deger%s\n",z,isfinite(z) ? " degildir":"dir");



	
	
	return 0;
}

